var Drawing = require('./Drawing');

exports.addVote = function (req, res) {
  var vote = req.params; // keep eye on
  // update drawing vote count - using image.Id as ref
}

exports.retrieveRoundVotes = function (req, res) {
  var vote = req.params; // keep eye on
  // update drawing vote count - using image.Id as ref
}